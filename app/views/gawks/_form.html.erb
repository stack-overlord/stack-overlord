<%= form_for(@gawk) do |f| %>
  <% if @gawk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gawk.errors.count, "error") %> prohibited this gawk from being saved:</h2>

      <ul>
      <% @gawk.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
