<%= form_for(@hashed_mac) do |f| %>
  <% if @hashed_mac.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hashed_mac.errors.count, "error") %> prohibited this hashed_mac from being saved:</h2>

      <ul>
      <% @hashed_mac.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
